<!DOCTYPE html>
<html>
  <head>
    <title>Pozyczarka</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%#= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
  </head>

  <body class="container">
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
        <p class="navbar-brand" href="#">Pożyczarka</p>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav">
            <%= link_to 'All', root_path, class: nav_link_class(root_path) %>
            <%= link_to 'Active', active_items_path, class: nav_link_class(active_items_path) %>
            <%= link_to 'Inactive', inactive_items_path, class: nav_link_class(inactive_items_path)  %>
            <%= link_to 'Borrowed', borrowed_items_path, class: nav_link_class(borrowed_items_path) %>
            <%= link_to 'On place', on_place_items_path, class: nav_link_class(on_place_items_path) %>
          </ul>
        </div>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
