<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <div class="my-2">
    <%= link_to "Back", items_path, class: 'btn btn-secondary' %>
  </div>

  <div class="my-2">
    <%= link_to 'Delete', item_path(@item), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
    <%#= button_to "Delete",  item_path(@item), method: :delete, class: 'btn btn-danger', form: { data: { turbo_confirm: 'Are you sure?' }}%>
  </div>
</div>
