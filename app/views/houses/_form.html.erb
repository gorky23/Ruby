<%= form_with(model: house) do |form| %>
  <% if house.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(house.errors.count, "error") %> prohibited this house from being saved:</h2>

      <ul>
        <% house.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :door_color, style: "display: block" %>
    <%= form.text_field :door_color %>
  </div>

  <div>
    <%= form.label :room_color, style: "display: block" %>
    <%= form.text_field :room_color %>
  </div>

  <div>
    <%= form.label :floor_color, style: "display: block" %>
    <%= form.text_field :floor_color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
